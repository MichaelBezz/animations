extends ../components/base/layout

block variables
  - const title = "Задачки на прокачку" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = false
  - const footerMods = ""
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    +intro()
    +section()(id="menu")
      +menu-block()
    +section()(id="loader")
      +loader-block()
    +section()(id="first-screen")
      +first-screen-block()
    +section()(id="show-on-scroll")
      +show-scroll-block()
    +section()(id="parallax")
      +parallax-block()

block modals
